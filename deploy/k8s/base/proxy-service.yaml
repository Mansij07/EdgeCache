apiVersion: v1
kind: Service
metadata:
  name: edgecache-proxy
  namespace: edgecache
  labels: { app: edgecache-proxy }
spec:
  selector: { app: edgecache-proxy }
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 9100
      targetPort: 9100
